---
phase: "06-queue-infrastructure-email-jobs"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/mail/mailables/queueable.interface.ts
  - backend/src/mail/mailables/decorators.ts
  - backend/src/mail/mailables/queueable-mail.ts
  - backend/src/mail/mailables/metadata-reader.ts
  - backend/src/mail/mailables/verification.mail.ts
  - backend/src/mail/mailables/reset-password.mail.ts
  - backend/src/mail/mailables/org-invite.mail.ts
  - backend/src/mail/mailables/welcome.mail.ts
  - backend/src/mail/mailables/application-submitted.mail.ts
  - backend/src/mail/mailables/status-changed.mail.ts
autonomous: true
requirements:
  - QUEUE-01
  - QUEUE-02

must_haves:
  truths:
    - "All 6 email types exist as Mailable subclasses with typed constructor arguments"
    - "Decorator metadata is readable via getQueueMetadata() returning queue, attempts, backoff"
    - "VerificationMail and ResetPasswordMail decorators route to emails-auth with 5 attempts exp 10s"
    - "ApplicationSubmittedMail and StatusChangedMail decorators route to emails-transactional with 6 attempts exp 30s"
  artifacts:
    - path: "backend/src/mail/mailables/queueable.interface.ts"
      provides: "Empty Queueable marker interface"
      exports: ["Queueable"]
    - path: "backend/src/mail/mailables/decorators.ts"
      provides: "Queue, Tries, Backoff class decorators using reflect-metadata"
      exports: ["Queue", "Tries", "Backoff", "QUEUE_META", "TRIES_META", "BACKOFF_META"]
    - path: "backend/src/mail/mailables/queueable-mail.ts"
      provides: "QueueableMail concrete base class"
      exports: ["QueueableMail"]
    - path: "backend/src/mail/mailables/metadata-reader.ts"
      provides: "getQueueMetadata helper that reads decorator metadata from a Mailable instance"
      exports: ["getQueueMetadata"]
    - path: "backend/src/mail/mailables/verification.mail.ts"
      provides: "VerificationMail Mailable"
      exports: ["VerificationMail"]
    - path: "backend/src/mail/mailables/reset-password.mail.ts"
      provides: "ResetPasswordMail Mailable"
      exports: ["ResetPasswordMail"]
    - path: "backend/src/mail/mailables/org-invite.mail.ts"
      provides: "OrgInviteMail Mailable"
      exports: ["OrgInviteMail"]
    - path: "backend/src/mail/mailables/welcome.mail.ts"
      provides: "WelcomeMail Mailable"
      exports: ["WelcomeMail"]
    - path: "backend/src/mail/mailables/application-submitted.mail.ts"
      provides: "ApplicationSubmittedMail Mailable"
      exports: ["ApplicationSubmittedMail"]
    - path: "backend/src/mail/mailables/status-changed.mail.ts"
      provides: "StatusChangedMail Mailable"
      exports: ["StatusChangedMail"]
  key_links:
    - from: "decorators.ts"
      to: "metadata-reader.ts"
      via: "QUEUE_META / TRIES_META / BACKOFF_META symbols"
      pattern: "Reflect\\.getMetadata\\(QUEUE_META"
    - from: "queueable-mail.ts"
      to: "verification.mail.ts"
      via: "class VerificationMail extends QueueableMail"
      pattern: "extends QueueableMail"
---

<objective>
Create the Mailable type system that underpins Phase 6. This plan establishes the three-layer architecture (marker interface, base class, decorator metadata) and implements all 6 Mailable subclasses.

Purpose: Every downstream plan depends on these types. Establishing them first prevents the executor from having to guess contracts.
Output: `backend/src/mail/mailables/` directory with 10 new files, zero existing files modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-queue-infrastructure-email-jobs/06-CONTEXT.md
</context>

<interfaces>
<!-- Key patterns the executor must follow. No codebase exploration needed. -->

<!-- Existing WorkerHost/BullMQ import style (from webhook.processor.ts): -->
```typescript
import { Processor, WorkerHost } from '@nestjs/bullmq';
import { Job } from 'bullmq';
```

<!-- reflect-metadata is already a transitive dependency of @nestjs/common.
     It is imported once in main.ts. No new package.json entry needed.
     Use Reflect.defineMetadata / Reflect.getMetadata directly. -->

<!-- D-01 canonical shapes (copy exactly): -->
// queueable.interface.ts
export interface Queueable {} // empty marker

// decorators.ts
export const QUEUE_META = Symbol('queue');
export const TRIES_META = Symbol('tries');
export const BACKOFF_META = Symbol('backoff');
export const Queue = (name: string): ClassDecorator =>
  (target) => Reflect.defineMetadata(QUEUE_META, name, target);
export const Tries = (attempts: number): ClassDecorator =>
  (target) => Reflect.defineMetadata(TRIES_META, attempts, target);
export const Backoff = (config: { type: 'exponential' | 'fixed'; delay: number }): ClassDecorator =>
  (target) => Reflect.defineMetadata(BACKOFF_META, config, target);

// queueable-mail.ts
export class QueueableMail implements Queueable {
  readonly template: string = '';
  readonly subject: string = '';
  readonly to: string = '';
  readonly context: Record<string, unknown> = {};
}

// metadata-reader.ts
export function getQueueMetadata(mail: QueueableMail) {
  const ctor = mail.constructor;
  return {
    queue: Reflect.getMetadata(QUEUE_META, ctor) ?? 'emails-auth',
    attempts: Reflect.getMetadata(TRIES_META, ctor) ?? 5,
    backoff: Reflect.getMetadata(BACKOFF_META, ctor) ?? { type: 'exponential', delay: 10_000 },
  };
}
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Mailable foundation files (interface, decorators, base class, metadata reader)</name>
  <read_first>
    backend/src/mail/mail.service.ts
    backend/src/notifications/webhook.processor.ts
  </read_first>
  <files>
    backend/src/mail/mailables/queueable.interface.ts
    backend/src/mail/mailables/decorators.ts
    backend/src/mail/mailables/queueable-mail.ts
    backend/src/mail/mailables/metadata-reader.ts
  </files>
  <behavior>
    - getQueueMetadata(new VerificationMail('a@b.com', {...})).queue === 'emails-auth'
    - getQueueMetadata(new VerificationMail('a@b.com', {...})).attempts === 5
    - getQueueMetadata(new ApplicationSubmittedMail('a@b.com', {...})).queue === 'emails-transactional'
    - getQueueMetadata(new ApplicationSubmittedMail('a@b.com', {...})).attempts === 6
    - getQueueMetadata applied to a plain QueueableMail instance returns defaults: queue='emails-auth', attempts=5, backoff.delay=10000
  </behavior>
  <action>
Create the following 4 files with exactly the shapes defined in the `<interfaces>` block above (D-01):

**backend/src/mail/mailables/queueable.interface.ts**
```typescript
export interface Queueable {} // empty marker interface — like Laravel's ShouldQueue
```

**backend/src/mail/mailables/decorators.ts**
```typescript
import 'reflect-metadata';

/**
 * BullMQ operational config decorators — direct equivalent of Laravel 13's
 * #[Queue], #[Tries], #[Backoff] PHP 8 attributes.
 *
 * Note: BullMQ backoff is { type: 'exponential' | 'fixed', delay: baseMs }.
 * Stepped arrays (Laravel #[Backoff([10, 30, 60])]) are not supported natively.
 */

export const QUEUE_META = Symbol('queue');
export const TRIES_META = Symbol('tries');
export const BACKOFF_META = Symbol('backoff');

export const Queue = (name: string): ClassDecorator =>
  (target) => Reflect.defineMetadata(QUEUE_META, name, target);

export const Tries = (attempts: number): ClassDecorator =>
  (target) => Reflect.defineMetadata(TRIES_META, attempts, target);

export const Backoff = (config: { type: 'exponential' | 'fixed'; delay: number }): ClassDecorator =>
  (target) => Reflect.defineMetadata(BACKOFF_META, config, target);
```

**backend/src/mail/mailables/queueable-mail.ts**
```typescript
import { Queueable } from './queueable.interface';

/**
 * QueueableMail — concrete base class for all email Mailables.
 * Mirrors Laravel's Mailable (concrete, not abstract).
 *
 * Context constraint: context values must be primitives only (string | number | boolean).
 * Never pass Prisma model instances — BullMQ serializes to JSON.
 */
export class QueueableMail implements Queueable {
  readonly template: string = '';
  readonly subject: string = '';
  readonly to: string = '';
  readonly context: Record<string, unknown> = {};
}
```

**backend/src/mail/mailables/metadata-reader.ts**
```typescript
import { QueueableMail } from './queueable-mail';
import { QUEUE_META, TRIES_META, BACKOFF_META } from './decorators';

export interface QueueMetadata {
  queue: string;
  attempts: number;
  backoff: { type: 'exponential' | 'fixed'; delay: number };
}

/**
 * getQueueMetadata — reads @Queue/@Tries/@Backoff decorator metadata from
 * a QueueableMail instance. Returns defaults if decorators are absent.
 *
 * Single indirection layer: if config strategy changes, update here only.
 */
export function getQueueMetadata(mail: QueueableMail): QueueMetadata {
  const ctor = mail.constructor;
  return {
    queue: Reflect.getMetadata(QUEUE_META, ctor) ?? 'emails-auth',
    attempts: Reflect.getMetadata(TRIES_META, ctor) ?? 5,
    backoff: Reflect.getMetadata(BACKOFF_META, ctor) ?? { type: 'exponential', delay: 10_000 },
  };
}
```
  </action>
  <verify>
    <automated>
      grep -r "export interface Queueable" /home/cativo23/projects/personal/kovia/backend/src/mail/mailables/queueable.interface.ts &&
      grep -r "export const QUEUE_META = Symbol" /home/cativo23/projects/personal/kovia/backend/src/mail/mailables/decorators.ts &&
      grep -r "export class QueueableMail implements Queueable" /home/cativo23/projects/personal/kovia/backend/src/mail/mailables/queueable-mail.ts &&
      grep -r "export function getQueueMetadata" /home/cativo23/projects/personal/kovia/backend/src/mail/mailables/metadata-reader.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - backend/src/mail/mailables/queueable.interface.ts exists and contains `export interface Queueable {}`
    - backend/src/mail/mailables/decorators.ts contains `export const QUEUE_META = Symbol('queue')`, `export const Queue`, `export const Tries`, `export const Backoff`
    - backend/src/mail/mailables/queueable-mail.ts contains `export class QueueableMail implements Queueable`
    - backend/src/mail/mailables/metadata-reader.ts contains `export function getQueueMetadata` and references `QUEUE_META`, `TRIES_META`, `BACKOFF_META`
  </acceptance_criteria>
  <done>Four foundation files exist and TypeScript compiles without errors: `cd backend && npx tsc --noEmit` passes for these files.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create all 6 Mailable subclasses</name>
  <read_first>
    backend/src/mail/mailables/queueable-mail.ts
    backend/src/mail/mailables/decorators.ts
    backend/src/mail/mailables/metadata-reader.ts
    backend/src/mail/mail.service.ts
  </read_first>
  <files>
    backend/src/mail/mailables/verification.mail.ts
    backend/src/mail/mailables/reset-password.mail.ts
    backend/src/mail/mailables/org-invite.mail.ts
    backend/src/mail/mailables/welcome.mail.ts
    backend/src/mail/mailables/application-submitted.mail.ts
    backend/src/mail/mailables/status-changed.mail.ts
  </files>
  <behavior>
    - VerificationMail: queue='emails-auth', attempts=5, backoff.delay=10000, template='verification', context has firstName+verificationUrl
    - ResetPasswordMail: queue='emails-auth', attempts=5, backoff.delay=10000, template='reset-password', context has firstName+resetUrl
    - OrgInviteMail: queue='emails-auth', attempts=5, backoff.delay=10000, template='org-invite', context has orgName+inviteUrl
    - WelcomeMail: queue='emails-auth', attempts=5, backoff.delay=10000, template='welcome', context has firstName
    - ApplicationSubmittedMail: queue='emails-transactional', attempts=6, backoff.delay=30000, template='application-submitted', context has firstName+animalName+orgName
    - StatusChangedMail: queue='emails-transactional', attempts=6, backoff.delay=30000, template='status-changed', context has firstName+animalName+newStatus
  </behavior>
  <action>
Create the 6 Mailable subclass files. Each follows the exact decorator+constructor pattern from D-02 in 06-CONTEXT.md.

**backend/src/mail/mailables/verification.mail.ts**
```typescript
import { Queue, Tries, Backoff } from './decorators';
import { QueueableMail } from './queueable-mail';

@Queue('emails-auth')
@Tries(5)
@Backoff({ type: 'exponential', delay: 10_000 })
export class VerificationMail extends QueueableMail {
  readonly template = 'verification';
  readonly subject = 'Verifica tu cuenta en Kovia';

  constructor(
    readonly to: string,
    readonly context: { firstName: string; verificationUrl: string },
  ) {
    super();
  }
}
```

**backend/src/mail/mailables/reset-password.mail.ts**
```typescript
import { Queue, Tries, Backoff } from './decorators';
import { QueueableMail } from './queueable-mail';

@Queue('emails-auth')
@Tries(5)
@Backoff({ type: 'exponential', delay: 10_000 })
export class ResetPasswordMail extends QueueableMail {
  readonly template = 'reset-password';
  readonly subject = 'Restablece tu contrasena en Kovia';

  constructor(
    readonly to: string,
    readonly context: { firstName: string; resetUrl: string },
  ) {
    super();
  }
}
```

**backend/src/mail/mailables/org-invite.mail.ts**
```typescript
import { Queue, Tries, Backoff } from './decorators';
import { QueueableMail } from './queueable-mail';

@Queue('emails-auth')
@Tries(5)
@Backoff({ type: 'exponential', delay: 10_000 })
export class OrgInviteMail extends QueueableMail {
  readonly template = 'org-invite';
  readonly subject = 'Has sido invitado a unirte a Kovia';

  constructor(
    readonly to: string,
    readonly context: { orgName: string; inviteUrl: string },
  ) {
    super();
  }
}
```

**backend/src/mail/mailables/welcome.mail.ts**
```typescript
import { Queue, Tries, Backoff } from './decorators';
import { QueueableMail } from './queueable-mail';

@Queue('emails-auth')
@Tries(5)
@Backoff({ type: 'exponential', delay: 10_000 })
export class WelcomeMail extends QueueableMail {
  readonly template = 'welcome';
  readonly subject = 'Bienvenido/a a Kovia';

  constructor(
    readonly to: string,
    readonly context: { firstName: string },
  ) {
    super();
  }
}
```

**backend/src/mail/mailables/application-submitted.mail.ts**
```typescript
import { Queue, Tries, Backoff } from './decorators';
import { QueueableMail } from './queueable-mail';

@Queue('emails-transactional')
@Tries(6)
@Backoff({ type: 'exponential', delay: 30_000 })
export class ApplicationSubmittedMail extends QueueableMail {
  readonly template = 'application-submitted';
  readonly subject = 'Tu solicitud de adopcion fue recibida';

  constructor(
    readonly to: string,
    readonly context: { firstName: string; animalName: string; orgName: string },
  ) {
    super();
  }
}
```

**backend/src/mail/mailables/status-changed.mail.ts**
```typescript
import { Queue, Tries, Backoff } from './decorators';
import { QueueableMail } from './queueable-mail';

@Queue('emails-transactional')
@Tries(6)
@Backoff({ type: 'exponential', delay: 30_000 })
export class StatusChangedMail extends QueueableMail {
  readonly template = 'status-changed';
  readonly subject = 'El estado de tu solicitud ha cambiado';

  constructor(
    readonly to: string,
    readonly context: { firstName: string; animalName: string; newStatus: string },
  ) {
    super();
  }
}
```
  </action>
  <verify>
    <automated>
      grep -r "extends QueueableMail" /home/cativo23/projects/personal/kovia/backend/src/mail/mailables/ | wc -l | grep -q "^6$" &&
      grep -r "@Queue('emails-transactional')" /home/cativo23/projects/personal/kovia/backend/src/mail/mailables/ | wc -l | grep -q "^2$" &&
      grep -r "@Queue('emails-auth')" /home/cativo23/projects/personal/kovia/backend/src/mail/mailables/ | wc -l | grep -q "^4$"
    </automated>
  </verify>
  <acceptance_criteria>
    - 6 files exist in backend/src/mail/mailables/, each containing a class that `extends QueueableMail`
    - 4 files contain `@Queue('emails-auth')`: verification, reset-password, org-invite, welcome
    - 2 files contain `@Queue('emails-transactional')`: application-submitted, status-changed
    - ApplicationSubmittedMail contains `@Tries(6)` and `@Backoff({ type: 'exponential', delay: 30_000 })`
    - VerificationMail contains `@Tries(5)` and `@Backoff({ type: 'exponential', delay: 10_000 })`
    - `cd backend && npx tsc --noEmit` exits 0 (all mailables compile)
  </acceptance_criteria>
  <done>All 6 Mailable files exist with correct decorators, constructor types, and template names. TypeScript compilation succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mailable context → BullMQ job payload | context values are serialized as JSON; must be primitives only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information Disclosure | QueueableMail context | mitigate | context type is `Record<string, unknown>` — Mailables enforce primitive-only values via TypeScript context types in each subclass constructor (e.g., `{ firstName: string; verificationUrl: string }`) which prevents Prisma model instances from being passed at compile time |
</threat_model>

<verification>
After both tasks:
- `ls backend/src/mail/mailables/*.ts | wc -l` outputs 10
- `cd backend && npx tsc --noEmit` exits 0 with no errors in the mailables directory
- `grep -r "extends QueueableMail" backend/src/mail/mailables/` returns 6 matches
- `grep -r "@Queue('emails-transactional')" backend/src/mail/mailables/` returns 2 matches
</verification>

<success_criteria>
- backend/src/mail/mailables/ contains exactly 10 .ts files
- All 6 Mailable subclasses compile without TypeScript errors
- Decorator metadata round-trip verified conceptually: @Queue/@Tries/@Backoff on each class, getQueueMetadata reads them back
- No changes to any existing file
</success_criteria>

<output>
After completion, create `.planning/phases/06-queue-infrastructure-email-jobs/06-01-SUMMARY.md`
</output>
