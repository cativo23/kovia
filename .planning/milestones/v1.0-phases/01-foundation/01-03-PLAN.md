---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/app/pages/login.vue
  - frontend/app/pages/register.vue
  - frontend/app/pages/verify-email.vue
  - frontend/app/pages/forgot-password.vue
  - frontend/app/pages/reset-password.vue
  - frontend/app/pages/auth/callback.vue
  - frontend/app/stores/auth.ts
  - frontend/app/composables/useAuth.ts
  - frontend/app/composables/useApi.ts
  - frontend/app/middleware/auth.ts
  - frontend/app/middleware/guest.ts
  - frontend/app/middleware/admin.ts
  - frontend/app/plugins/api.ts
  - frontend/app/layouts/default.vue
  - frontend/app/layouts/admin.vue
  - frontend/app/layouts/auth.vue
  - frontend/i18n/locales/es-SV.json
  - frontend/tests/unit/stores/auth.spec.ts
  - frontend/tests/unit/composables/useApi.spec.ts
  - frontend/tests/unit/middleware/auth.spec.ts
autonomous: true
requirements:
  - INFR-02

must_haves:
  truths:
    - "Login page renders in Spanish with email/password form and Google OAuth button equally prominent"
    - "Register page creates account and shows 'check your email' message"
    - "Verify email page auto-verifies on load (magic link) and redirects to dashboard"
    - "Forgot password page accepts email and shows confirmation"
    - "Reset password page accepts new password (from magic link) and logs user in"
    - "Auth state persists across page refresh via silent token refresh"
    - "Protected routes redirect to login, guest routes redirect away if logged in"
    - "All UI text uses i18n keys in es-SV locale"
    - "Auth store, useApi composable, and middleware have unit test coverage at 80%+"
  artifacts:
    - path: "frontend/app/stores/auth.ts"
      provides: "Pinia auth store with token management"
      exports: ["useAuthStore"]
    - path: "frontend/app/composables/useApi.ts"
      provides: "API client with auto-auth headers and token refresh"
      exports: ["useApi"]
    - path: "frontend/app/pages/login.vue"
      provides: "Login page with email/password and Google OAuth"
    - path: "frontend/app/layouts/admin.vue"
      provides: "Admin layout with sidebar navigation"
    - path: "frontend/tests/unit/stores/auth.spec.ts"
      provides: "Auth store unit tests"
    - path: "frontend/tests/unit/composables/useApi.spec.ts"
      provides: "useApi composable unit tests"
  key_links:
    - from: "frontend/app/stores/auth.ts"
      to: "backend /auth/* endpoints"
      via: "useApi composable fetch calls"
      pattern: "api.*auth"
    - from: "frontend/app/middleware/auth.ts"
      to: "frontend/app/stores/auth.ts"
      via: "Check isAuthenticated before route access"
      pattern: "useAuthStore.*isAuthenticated"
    - from: "frontend/app/composables/useApi.ts"
      to: "backend /auth/refresh"
      via: "Auto-refresh on 401 response"
      pattern: "auth/refresh"
---

<objective>
Build all frontend auth pages, the Pinia auth store with silent token refresh, API client plugin, route middleware, layouts, complete Spanish i18n translations, and unit tests for frontend business logic.

Purpose: The backend auth API (Plan 02) needs a frontend. Users need visual pages to register, log in, verify email, and reset passwords. The auth store and API client are foundational -- every future frontend page will use them. Unit tests ensure the business logic in store/composables/middleware is reliable.

Output: Complete auth UI flow in Spanish, three layouts (default, auth, admin), route protection middleware, a robust auth store with automatic token refresh, and 80%+ frontend unit test coverage on business logic.
</objective>

<execution_context>
@/home/cativo23/.claude/get-shit-done/workflows/execute-plan.md
@/home/cativo23/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API client, auth store, middleware, and layouts</name>
  <files>
    frontend/app/plugins/api.ts, frontend/app/composables/useApi.ts,
    frontend/app/stores/auth.ts, frontend/app/composables/useAuth.ts,
    frontend/app/middleware/auth.ts, frontend/app/middleware/guest.ts, frontend/app/middleware/admin.ts,
    frontend/app/layouts/default.vue, frontend/app/layouts/auth.vue, frontend/app/layouts/admin.vue,
    frontend/i18n/locales/es-SV.json
  </files>
  <action>
    1. Create API client plugin (`frontend/app/plugins/api.ts`):
       - Uses `$fetch` from Nuxt (ofetch) configured with baseURL from runtimeConfig.public.apiUrl
       - Attaches Authorization Bearer header from auth store's access token
       - On 401 response: attempts silent refresh via /auth/refresh (credentials: 'include' for cookie), retries original request
       - If refresh also fails: clear auth state, redirect to /login
       - Export as `$api` available via `useNuxtApp()`

    2. Create `useApi` composable (`frontend/app/composables/useApi.ts`):
       - Wrapper around the $api plugin for convenient use in components
       - Typed methods: get<T>, post<T>, put<T>, delete<T>
       - Handles loading state and error state

    3. Create Pinia auth store (`frontend/app/stores/auth.ts`):
       - State: user (User | null), accessToken (string | null), isInitialized (boolean)
       - Getters: isAuthenticated, isAdmin, isOrgAdmin, userRole
       - Actions:
         - `register(dto)`: POST /auth/register, does NOT auto-login (user must verify email)
         - `login(email, password)`: POST /auth/login (credentials: 'include'), stores access token in memory + user data
         - `loginWithGoogle()`: redirects to API_URL/auth/google (full page redirect)
         - `handleOAuthCallback(token)`: stores access token from query param, fetches user profile via GET /auth/me
         - `verifyEmail(token)`: POST /auth/verify-email, stores returned tokens
         - `forgotPassword(email)`: POST /auth/forgot-password
         - `resetPassword(token, password)`: POST /auth/reset-password, stores returned tokens
         - `refreshToken()`: POST /auth/refresh (credentials: 'include'), updates access token
         - `logout()`: POST /auth/logout (credentials: 'include'), clear state, redirect to /login
         - `initialize()`: try refreshToken silently on app load (restores session after page refresh per AUTH-04)
       - Access token stored ONLY in memory (Pinia state) -- never localStorage (per research anti-patterns)
       - Refresh token managed as httpOnly cookie by backend -- frontend never reads it directly

    4. Create `useAuth` composable:
       - Convenience wrapper over auth store for template use
       - Returns reactive refs: user, isAuthenticated, isAdmin

    5. Create route middleware:
       - `auth.ts` (named middleware): if not authenticated AND not initialized, try initialize(); if still not authenticated, redirect to /login
       - `guest.ts` (named middleware): if authenticated, redirect to / (prevents logged-in users from seeing login/register)
       - `admin.ts` (named middleware): if not admin role, redirect to / with error toast

    6. Create layouts:
       - `auth.vue`: Centered card layout for login/register/verify/reset pages. Logo at top, card in center, minimal footer. Uses Nuxt UI components.
       - `default.vue`: Standard app layout with top navbar showing: logo, nav links, user menu dropdown (profile, logout) or login/register buttons. Responsive.
       - `admin.vue`: Admin layout with left sidebar nav (Dashboard, Organizaciones, Usuarios, Invitaciones) and main content area. Shows "Panel de Administracion" header. Per CONTEXT.md: separate /admin route with its own layout.

    7. Update `frontend/i18n/locales/es-SV.json` with comprehensive translations for all auth flows, layouts, common UI elements, and error messages. Include:
       - auth.* (login, register, verify, reset, errors, validation messages)
       - common.* (loading, save, cancel, delete, confirm, back, error, success)
       - nav.* (home, login, register, logout, profile, admin)
       - admin.* (dashboard, orgs, users, invites, stats)
       - org.* (profile fields, status labels)
       - validation.* (required, email, minLength, maxLength, passwordMismatch)
       All in Salvadoran Spanish (vos form where applicable: "verifica", "ingresa", "registrate").
  </action>
  <verify>
    <automated>docker compose exec web npx nuxt build 2>&1 | tail -20</automated>
  </verify>
  <done>API client plugin works with auth headers. Pinia auth store manages access token in memory and refresh via httpOnly cookie. Three layouts render correctly. All middleware guards routes properly. i18n translations cover all auth and common UI strings in es-SV.</done>
</task>

<task type="auto">
  <name>Task 2: Auth pages (login, register, verify, reset, OAuth callback)</name>
  <files>
    frontend/app/pages/login.vue, frontend/app/pages/register.vue,
    frontend/app/pages/verify-email.vue, frontend/app/pages/forgot-password.vue,
    frontend/app/pages/reset-password.vue, frontend/app/pages/auth/callback.vue,
    frontend/app/pages/index.vue
  </files>
  <action>
    All pages use Nuxt UI components (UButton, UInput, UCard, UForm, etc.), i18n $t() for all visible text, and the auth layout.

    1. `login.vue` (layout: auth, middleware: guest):
       - Email + password form with validation (class: UForm with Zod or yup schema)
       - "Iniciar sesion" submit button
       - Google OAuth button equally prominent: "Continuar con Google" with Google icon. Calls authStore.loginWithGoogle()
       - Link to /register: "No tengo cuenta? Crear cuenta"
       - Link to /forgot-password: "Olvide mi contrasena"
       - Error toast on failed login (using Nuxt UI toast/notification)
       - On success: redirect to / (or /admin if admin role)

    2. `register.vue` (layout: auth, middleware: guest):
       - Form fields: firstName, lastName, email, password, confirmPassword
       - Validation: email format, password min 8 chars with complexity, passwords match
       - Google OAuth button: "Registrarse con Google"
       - On success: show "Revisa tu correo electronico" message with envelope icon, instructions to check Mailpit in dev
       - Link to /login: "Ya tengo cuenta? Iniciar sesion"

    3. `verify-email.vue` (layout: auth):
       - Reads `token` from query params on mount
       - Auto-calls authStore.verifyEmail(token)
       - Shows loading spinner while verifying
       - On success: show success message, auto-redirect to / after 2 seconds
       - On TOKEN_EXPIRED error: show "Este enlace ha expirado" with "Enviar nuevo enlace" button that calls resendVerification (per CONTEXT.md locked decision)
       - On other error: show generic error message

    4. `forgot-password.vue` (layout: auth, middleware: guest):
       - Email input form
       - On submit: call authStore.forgotPassword(email)
       - Show confirmation: "Si tu correo esta registrado, recibiras un enlace para restablecer tu contrasena"
       - Link back to /login

    5. `reset-password.vue` (layout: auth):
       - Reads `token` from query params
       - Shows "set new password" form: newPassword, confirmPassword
       - Validation: min 8 chars, complexity, match
       - On success: auto-login (stores tokens from response), redirect to /
       - On TOKEN_EXPIRED: show "Este enlace ha expirado" with "Enviar nuevo enlace" button (per CONTEXT.md)

    6. `auth/callback.vue` (layout: auth):
       - Reads `token` from query params (set by backend Google OAuth redirect)
       - Calls authStore.handleOAuthCallback(token)
       - Shows loading spinner
       - On success: redirect to /
       - On error: redirect to /login with error message

    7. Update `index.vue` (layout: default):
       - Simple landing/home page
       - If authenticated: show "Bienvenido, {{user.firstName}}" with role info
       - If not authenticated: show welcome message with login/register CTAs
       - This is a placeholder -- will be replaced in later phases
  </action>
  <verify>
    <automated>docker compose exec web npx nuxt build 2>&1 | tail -20 && curl -s http://localhost:3001/login | grep -c "Iniciar" && curl -s http://localhost:3001/register | grep -c "Crear"</automated>
  </verify>
  <done>All 7 auth pages render in Spanish with Nuxt UI components. Login shows email/password form and Google OAuth button. Register shows full form with validation. Verify-email handles magic link auto-verification with expired link resend. Reset-password handles magic link with new password form. OAuth callback captures token from redirect. All pages use i18n keys. All forms have validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Frontend unit tests for auth store, useApi, and middleware</name>
  <files>
    frontend/tests/unit/stores/auth.spec.ts,
    frontend/tests/unit/composables/useApi.spec.ts,
    frontend/tests/unit/middleware/auth.spec.ts
  </files>
  <behavior>
    Auth store tests (frontend/tests/unit/stores/auth.spec.ts):
    - login: calls POST /auth/login with credentials, stores accessToken and user in state
    - login: sets isAuthenticated getter to true after successful login
    - login: throws and does not modify state on API error
    - logout: calls POST /auth/logout, clears accessToken and user
    - register: calls POST /auth/register, does NOT set accessToken (user must verify)
    - verifyEmail: calls POST /auth/verify-email, stores returned tokens (auto-login)
    - resetPassword: calls POST /auth/reset-password, stores returned tokens (auto-login)
    - refreshToken: calls POST /auth/refresh, updates accessToken
    - refreshToken: clears state and does NOT throw when refresh fails (silent failure)
    - initialize: calls refreshToken on first call, sets isInitialized=true
    - initialize: does not call refreshToken on subsequent calls (idempotent)
    - isAdmin getter: returns true when user.role === 'PLATFORM_ADMIN'
    - isOrgAdmin getter: returns true when user.role === 'ORG_ADMIN'

    useApi composable tests (frontend/tests/unit/composables/useApi.spec.ts):
    - get: calls $fetch with GET method and auth header
    - post: calls $fetch with POST method, body, and auth header
    - 401 response: triggers token refresh and retries the original request
    - 401 response after failed refresh: clears auth state, redirects to /login
    - non-401 error: throws without attempting refresh

    Auth middleware tests (frontend/tests/unit/middleware/auth.spec.ts):
    - authenticated user: allows navigation (no redirect)
    - unauthenticated, uninitialized: calls initialize(), then allows if authenticated
    - unauthenticated after initialize: redirects to /login
    - guest middleware: authenticated user redirected to /
    - guest middleware: unauthenticated user allowed through
    - admin middleware: non-admin user redirected to /
    - admin middleware: PLATFORM_ADMIN user allowed through
  </behavior>
  <action>
    1. Create `frontend/tests/unit/stores/auth.spec.ts`:
       - Use Vitest + Pinia testing utilities (createTestingPinia or setActivePinia)
       - Mock `$fetch` / `useNuxtApp().$api` to simulate API responses
       - Test all store actions: login, logout, register, verifyEmail, resetPassword, refreshToken, initialize
       - Test all getters: isAuthenticated, isAdmin, isOrgAdmin, userRole
       - Verify accessToken is stored in state (memory), never in localStorage
       - Verify login with credentials: 'include' for cookie transport

    2. Create `frontend/tests/unit/composables/useApi.spec.ts`:
       - Mock $fetch and auth store
       - Test that Authorization header is attached with Bearer token
       - Test 401 retry logic: first request fails 401, refresh is called, original request retried
       - Test 401 cascade: refresh also fails, auth state cleared, navigation to /login
       - Test non-401 errors passed through without refresh attempt

    3. Create `frontend/tests/unit/middleware/auth.spec.ts`:
       - Mock useAuthStore and navigateTo
       - Test auth middleware: allows authenticated, initializes + checks uninitialized, redirects unauthenticated
       - Test guest middleware: redirects authenticated, allows unauthenticated
       - Test admin middleware: allows PLATFORM_ADMIN, redirects others

    4. Run all frontend tests and verify 80%+ coverage on stores/auth.ts, composables/useApi.ts, and middleware/*.ts.
  </action>
  <verify>
    <automated>docker compose exec web npx vitest run tests/unit/ --reporter=verbose --coverage 2>&1</automated>
  </verify>
  <done>Frontend unit tests pass for auth store (13 tests), useApi composable (5 tests), and middleware (7 tests). Coverage at 80%+ for frontend business logic files (stores/auth.ts, composables/useApi.ts, middleware/auth.ts, middleware/guest.ts, middleware/admin.ts).</done>
</task>

</tasks>

<verification>
1. `curl http://localhost:3001/login` -- renders Spanish login page with "Iniciar sesion" and Google OAuth button
2. `curl http://localhost:3001/register` -- renders registration form in Spanish
3. `curl http://localhost:3001/forgot-password` -- renders forgot password form in Spanish
4. `docker compose exec web npx nuxt build` -- builds without errors
5. `docker compose exec web npx vitest run tests/unit/ --reporter=verbose --coverage` -- all unit tests pass with 80%+ coverage
6. Navigate to localhost:3001/login in browser -- all i18n strings render in Spanish
7. All auth page flows redirect correctly based on authentication state
</verification>

<success_criteria>
- All auth pages render correctly in Spanish (es-SV)
- Login page shows email/password AND Google OAuth as equally prominent options
- Register flow shows "check your email" confirmation after success
- Verify-email auto-verifies on magic link click, shows resend on expired links
- Reset-password handles magic link and auto-logs user in
- Auth store persists session across page refresh via silent token refresh
- Protected routes redirect to login, guest routes redirect away if authenticated
- Admin layout has sidebar navigation for /admin routes
- All visible text uses i18n keys (no hardcoded strings)
- Frontend unit tests pass with 80%+ coverage on auth store, useApi, and middleware
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
